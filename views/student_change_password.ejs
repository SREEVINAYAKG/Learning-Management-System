<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%=csrfToken%>" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title><%=title%></title>
</head>
<body>
    <div class="grid grid-cols-6 grid-rows-10">
    <div class="col-start-1 col-span-6">
        <%- include('header.ejs')%>
    </div>
    <div class="col-start-1 col-span-6 row-start-2">
      <h2 class="text-2xl font-semibold pl-2 text-gray-600">Welcome <%=name%></h2>
      <a href="/student_dashboard" class="px-3 text-blue-800">Dashboard</a>
    </div>
    <div class="col-start-6 row-start-2 col-span-1 text-center px-2">
        <a href="/signout" class="text-red-500">Sign out</a>
    </div>
         <div class="col-start-2 row-start-3 col-span-4 row-span-8">
<form action="/student_dashboard/change_password" method="POST" class="max-w-md space-y-4">
  <input type="hidden" name="_csrf" value="<%= csrfToken %>">

  <div>
    <label class="block font-semibold">Current Password</label>
    <input type="password" name="currentPassword" required class="w-full border px-4 py-2 rounded">
  </div>

  <div>
    <label class="block font-semibold">New Password</label>
    <input type="password" name="newPassword" required class="w-full border px-4 py-2 rounded">
  </div>

  <div>
    <label class="block font-semibold">Confirm New Password</label>
    <input type="password" name="confirmPassword" required class="w-full border px-4 py-2 rounded">
  </div>

  <button type="submit" class="bg-blue-500 text-white px-6 py-2 rounded hover:bg-blue-600">
    Change Password
  </button>
</form>

<% if (messages.error) { %>
  <p class="text-red-500 mt-2"><%= messages.error %></p>
<% } %>
<% if (messages.success) { %>
  <p class="text-green-600 mt-2"><%= messages.success %></p>
<% } %>
      </div>

    </div>
        
    </body>
</html>