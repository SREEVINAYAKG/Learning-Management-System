<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%=csrfToken%>" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title><%=title%></title>
</head>
<body>
    <div class="grid grid-cols-6 grid-rows-10">
    <div class="col-start-1 col-span-6">
        <%- include('header.ejs')%>
    </div>
    <div class="col-start-1 col-span-6 row-start-2">
      <h2 class="text-2xl font-semibold pl-2 text-gray-600">Welcome <%=name%></h2>
      <a href="/educator_dashboard" class="px-3 text-blue-800">Dashboard</a>
    </div>
    <div class="col-start-6 row-start-2 col-span-1 text-center px-2">
        <a href="/signout" class="text-red-500">Sign out</a>
    </div>
         <div class="col-start-2 row-start-3 col-span-4 row-span-8">
        <form action="/educator_dashboard/<%= courseId %>/<%= chapterId %>/pages/page/<%= page.id %>/edit" method="POST">
            <input type="hidden" name="_csrf" value="<%=csrfToken%>">
            <label for="pageTitle">Page Title</label>
            <input name="pageTitle" class="border border-gray-300 text-gray-900 rounded w-full p-2" value="<%= page.pageTitle %>" required><br><br>
            <label for="pageCotent">Page Content</label>
            <textarea name="pageCotent" placeholder='enter page content' class="border border-gray-300 text-gray-900 rounded w-full h-32 p-2"><%= page.pageCotent %></textarea><br><br>
            <button type="submit" class="p-2 bg-yellow-200 border rounded hover:bg-yellow-400 cursor-pointer">Update</button>
        </form>

      </div>

    </div>
        
    </body>
</html>